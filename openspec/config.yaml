schema: spec-driven

# Project context (optional)
context: |
  Language Preference: All documents (proposals, specs, designs, tasks) must be written in Traditional Chinese (Taiwan).
  Tech stack: Next.js 15+, Tailwind CSS, PostgreSQL, AI Integration.

# Per-artifact rules (optional)
rules:
  proposal:
    - Use Traditional Chinese (Taiwan).
  specs:
    - Use Traditional Chinese (Taiwan).
  design:
    - Use Traditional Chinese (Taiwan).
  tasks:
    - Use Traditional Chinese (Taiwan).
