# No Delta Specs Required

This change only fixes bugs in existing code and does not modify any requirements.

Modified capabilities (implementation-level fixes only):
- `authentication`: Fixed SessionProvider wrapper
- `project-setup`: Fixed middleware export

No spec-level changes needed.
